@model SettingsVM

@{
    ViewData["Title"] = "Налаштування рахунків";
}

<h2 style="margin-top: 3rem;"><center>Налаштування рахунків</center></h2>

<hr />

<center>
    <table class="table" style="width: 60%;">
        <thead>
            <tr>
                <th>
                    <span style="margin-left: 4.5rem;">Рахунок</span>
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Accounts)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        <div style="display: flex; justify-content: center;">
                            <a asp-action="EditAccount" asp-route-id="@item.Id" class="btn btn-info" style="margin-right: 1rem;">Редагувати</a> |
                            <a asp-action="DeleteAccount" asp-route-id="@item.Id" class="btn btn-danger" style="margin-left: 1rem;">Видалити</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</center>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
